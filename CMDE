venv :
source .venv/bin/activate

Lancement dans (venv): python3 -m app.main

git status
git add .
git diff --cached --name-only
git commit -m "test: align gelato payload test with template fetch flow"
git push

Lancement test : python3 -m black app/ tests/
